import React from 'react';

const WebInfo = () => (
  <section className="webinfo-section py-5 text-black">
    <div className="container">
      <h2 className="mb-4">Web Info</h2>
      <p>
        This is the WebInfo component where you can highlight features or updates about your web app.
      </p>
    </div>
  </section>
);

export default WebInfo;

